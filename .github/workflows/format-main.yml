name: Formatting Repair

on:
    push:
        branches:
            - main
            - master
    workflow_dispatch:

concurrency:
    group: formatting-repair-${{ github.ref }}
    cancel-in-progress: true

permissions:
    contents: write
    pull-requests: write

jobs:
    create-formatting-pr:
        name: Create formatting repair PR
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: Check out repository
              uses: actions/checkout@v7
              with:
                  fetch-depth: 0

            - name: Set up Node.js
              uses: actions/setup-node@v7
              with:
                  node-version: 24
                  cache: npm

            - name: Install dependencies
              run: npm ci --ignore-scripts

            - name: Apply formatting
              run: npm run format

            - name: Create or update formatting pull request
              uses: peter-evans/create-pull-request@v8.1.1
              with:
                  token: ${{ secrets.GITHUB_TOKEN }}
                  base: ${{ github.ref_name }}
                  branch: chore/automated-formatting
                  delete-branch: true
                  commit-message: 'style: apply automated formatting'
                  committer: 'github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>'
                  author: 'github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>'
                  title: 'style: apply automated formatting'
                  body: |
                      Automated formatting repair generated after changes reached the default branch.

                      This pull request only contains changes produced by `npm run format`.
